apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: telemetry-database-dev
  annotations:
    tailscale.com/expose: "true"
    tailscale.com/hostname: backend-dev
spec:
  selector:
    app: backend
  ports:
    - name: http
      port: 8000
      targetPort: http