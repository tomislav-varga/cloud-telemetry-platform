apiVersion: v1
kind: Service
metadata:
  name: postgresql
  namespace: telemetry-database-dev
spec:
  clusterIP: None
  selector:
    app: postgresql
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
