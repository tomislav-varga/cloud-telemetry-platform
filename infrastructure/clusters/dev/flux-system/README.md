# Flux Bootstrap Placeholders

This folder is reserved for Flux bootstrap-managed files:

- `gotk-components.yaml`
- `gotk-sync.yaml`
- `kustomization.yaml`

If not present yet, bootstrap Flux with this path as needed.
